logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            DataSourceTransactionManager: DEBUG
        transaction:
          interceptor: TRACE
        orm:
          jpa:
            JpaTransactionManager: DEBUG
      hibernate:
        resource:
          transaction: DEBUG
        SQL: DEBUG